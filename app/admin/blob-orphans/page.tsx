import BlobOrphansView from "./blob-orphans-view";

export const metadata = { title: "Blob orphans — Panel Haus / Curator" };

export default function BlobOrphansPage() {
  return <BlobOrphansView />;
}
